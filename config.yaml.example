database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: lotus_car
  sslmode: disable

server:
  address: :8080

deal:
  boost_path: /Users/max/workspace/filecoin/boost/boost --repo=/Users/max/.boost-client
  deal_delay: 1000  # 发单间隔时间（毫秒）

auth:
  jwt_secret: "your-secret-key"  # 在生产环境中使用强密钥
  token_expire_hours: 2400       # token 有效期（小时）